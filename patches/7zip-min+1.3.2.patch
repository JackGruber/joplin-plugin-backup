diff --git a/node_modules/7zip-min/index.js b/node_modules/7zip-min/index.js
index e69bb90..ba20272 100644
--- a/node_modules/7zip-min/index.js
+++ b/node_modules/7zip-min/index.js
@@ -1,7 +1,16 @@
 'use strict';
 
 const spawn = require('child_process').spawn;
-const path7za = require('7zip-bin').path7za;
+let path7za = require('7zip-bin').path7za;
+
+function setPath7za(pathToBins) {
+    path7za = pathToBins
+}
+
+function getPath7za() {
+    return path7za
+}
+
 
 /**
  * Unpack archive.
@@ -60,6 +69,8 @@ function run(bin, args, cb) {
         let result = null;
         if (args[0] === 'l') {
             result = parseListOutput(output);
+        } else if ( args[0] === 't') {
+            result = parseListOutput(output)[0];
         }
         cb(code ? new Error('Exited with code ' + code) : null, result);
     });
@@ -95,6 +106,11 @@ function parseListOutput(str) {
         'Method': 'method',
         'Block': 'block',
         'Encrypted': 'encrypted',
+        'Type': 'type',
+        'Physical Size': 'physicalSize',
+        'Headers Size': 'headersSize',
+        'Solid': 'solid',
+        'Blocks': 'blocks',
     };
 
     if (!items.length) return [];
@@ -125,6 +141,8 @@ function parseListOutput(str) {
     return res;
 }
 
+exports.setPath7za = setPath7za;
+exports.getPath7za = getPath7za;
 exports.unpack = unpack;
 exports.pack = pack;
 exports.list = list;
